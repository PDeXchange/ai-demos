ARG BASE_UBI_IMAGE_TAG=9.5-1742914212

FROM registry.access.redhat.com/ubi9/ubi-minimal:${BASE_UBI_IMAGE_TAG} AS builder

ARG PYTHON_VERSION=3.12
ENV VIRTUAL_ENV=/opt/venv
ENV PATH=${VIRTUAL_ENV}/bin:$PATH

RUN microdnf install -y dnf openssl-devel && dnf install -y \
    gcc-toolset-13 gcc-toolset-13-gcc-c++ gcc-gfortran \
    openblas-devel python${PYTHON_VERSION}-devel python${PYTHON_VERSION}-pip \
    && dnf clean all \
    && python${PYTHON_VERSION} -m venv ${VIRTUAL_ENV} \
    && python -m pip install -U pip uv \
    && uv pip install wheel build setuptools 'cmake<4' cython

WORKDIR /fraud_detection
COPY requirements.txt /fraud_detection/

RUN  export PATH="$HOME/.cargo/bin:$PATH" && \
    source ${VIRTUAL_ENV}/bin/activate && \
    /opt/venv/bin/pip3.12 install --prefer-binary --extra-index-url=https://wheels.developerfirst.ibm.com/ppc64le/linux -r requirements.txt && \
    /opt/venv/bin/pip3.12 install --prefer-binary --extra-index-url=https://wheels.developerfirst.ibm.com/ppc64le/linux libprotobuf==4.25.8

FROM registry.access.redhat.com/ubi9/ubi-minimal:${BASE_UBI_IMAGE_TAG}
RUN microdnf install -y dnf && dnf install -y \
  python3.12 python3.12-pip make && dnf clean all

COPY --from=builder /opt/venv /opt/venv
COPY --from=builder /usr/local/lib /usr/local/lib
COPY --from=builder /usr/lib64 /usr/lib64
COPY --from=builder /fraud_detection /fraud_detection
COPY train.py /fraud_detection/
COPY Makefile config.pbtxt sample-fraud.json sample-non-fraud.json /fraud_detection/

ENV VIRTUAL_ENV=/opt/venv
ENV PATH=${VIRTUAL_ENV}/bin:$PATH

WORKDIR /fraud_detection

CMD ["/bin/bash", "-c", "export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/lib64:/usr/local/lib:/opt/venv/lib/python3.12/site-packages/libprotobuf/lib64/ && make train && make prepare"]
